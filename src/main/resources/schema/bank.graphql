type Bank {
    id: Int!
    bankName: String!
    bankAccount: String!
    editBy: String!
    editDate: String!
    createdBy: String!
    createsDate: String!
}

type Query {
    allBanks: [Bank]
    oneBank(id: ID): Bank
}

schema {
    query: Query
}
